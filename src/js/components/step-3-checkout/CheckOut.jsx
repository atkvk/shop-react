"use strict";

var React = require('react');

var Checkout = React.createClass({
    render: function () {
        return (
            <div>Checkout</div>
        );
    }
});

module.exports = Checkout;